# MongoDB connection string from Railway
# This is the connection URL for your MongoDB instance on Railway
MONGODB_URI=mongodb://centerbeam.proxy.rlwy.net:26289

# MongoDB database name
MONGODB_DATABASE=collaborative_editor

# Optional: Server port (defaults to 8887 if not set)
PORT=8887

# Railway environment indicator
RAILWAY_ENVIRONMENT=production 